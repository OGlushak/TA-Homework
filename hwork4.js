// //Task1
// function calcRectangleArea() {
//     let width = 3;
//     let height = 5;
//     let area = width * height;
//     console.log(`Area of rectangle is ` + area);
// }
// calcRectangleArea()

// //Task2
// const userAge = 12;

// let case1 = (" ");
// throw new SyntaxError1(`The field is empty! Please enter your age.`);

// let case2 = "abc";
// throw new SyntaxError2(`Please enter your age in numbers.`);

// let case3 = (userAge <= 14);
// throw new SyntaxError3(`Access is denied due age restriction.`);

const userAge = 12;
const userName = 'Josh';

function checkAge() {

    const message = prompt(`Please enter your age.`);
    try {
        if (case1 = " ") {
            throw SyntaxError(`The field is empty! Please enter your age.`);
        } catch (e) {
            console.log()
        } else if (case2 = "abc") {
            throw SyntaxError(`Please enter your age in numbers.`);
        } else if (case3 = userAge <= 14) {
            throw SyntaxError(`Access is denied due age restriction.`)
        }
    } catch (c) {

    }

}
checkAge()